# Task 01: Create `src-tauri/src/constants.rs`

## Action
- 将 `lib.rs` 中纯常量与固定片段字符串迁移到 `constants.rs`。

## Rules
- 只移动，不重写逻辑。
- 常量命名保持不变。

## Done When
- `lib.rs` 对应常量已通过 `use crate::constants::*` 引入。
- 编译通过。
