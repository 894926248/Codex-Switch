:root {
  --bg: #eceff1;
  --panel: #f6f7f8;
  --card-bg: linear-gradient(90deg, #f6f9f8 0%, #eff4f3 100%);
  --teal: #3f879d;
  --brand: #53bf8a;
  --text: #17252d;
  --muted: #5f7681;
  --line: #d4dee3;
  --line-soft: #e4ecef;
  --accent: #4cb087;
  --warn: #d55a5a;
  --shadow: 0 10px 30px rgba(57, 94, 110, 0.08);
  font-family: "Segoe UI", "Microsoft YaHei UI", sans-serif;
}

* {
  box-sizing: border-box;
}

html,
body,
#root {
  margin: 0;
  width: 100%;
  height: 100%;
  color: var(--text);
  background: var(--bg);
}

button {
  font-family: inherit;
}

.app-root {
  height: 100%;
  display: flex;
  flex-direction: column;
  background: var(--bg);
}

.top-bar {
  height: 68px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 18px;
  background: var(--teal);
  border-bottom: 1px solid #76a8b7;
}

.top-left,
.top-right {
  display: flex;
  align-items: center;
  gap: 12px;
}

.spark {
  color: #ffbb55;
  font-size: 16px;
  transform: rotate(8deg);
}

.brand {
  color: var(--brand);
  font-size: 24px;
  font-weight: 700;
  letter-spacing: 0.2px;
}

.header-icon,
.quick-btn,
.card-action,
.mini-icon {
  border: 1px solid #d4dde2;
  background: rgba(255, 255, 255, 0.82);
  color: #354750;
  border-radius: 10px;
  cursor: pointer;
  transition:
    background 0.16s ease,
    border-color 0.16s ease,
    transform 0.16s ease,
    opacity 0.16s ease;
}

.header-icon:hover,
.quick-btn:hover,
.card-action:hover,
.mini-icon:hover {
  border-color: #b9cbd3;
  background: #ffffff;
  transform: translateY(-1px);
}

.header-icon {
  width: 34px;
  height: 34px;
  border-radius: 11px;
  font-size: 16px;
  line-height: 1;
}

.add-btn {
  width: 40px;
  height: 40px;
  border: 0;
  border-radius: 999px;
  background: #ea8b2f;
  color: #fff;
  font-size: 28px;
  line-height: 1;
  cursor: pointer;
  transition: transform 0.2s ease, filter 0.2s ease;
}

.add-btn:hover {
  transform: translateY(-1px) scale(1.03);
  filter: brightness(1.05);
}

.add-btn:disabled,
.header-icon:disabled,
.quick-btn:disabled,
.card-action:disabled,
.mini-icon:disabled {
  opacity: 0.55;
  cursor: not-allowed;
  transform: none;
}

.keepalive-switch {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 0 8px;
  user-select: none;
}

.keepalive-switch input {
  position: absolute;
  opacity: 0;
  pointer-events: none;
}

.switch-track {
  width: 56px;
  height: 30px;
  border-radius: 999px;
  background: #d5dee2;
  border: 1px solid #c3d0d6;
  padding: 2px;
  display: inline-flex;
  align-items: center;
  transition: background 0.18s ease;
}

.switch-knob {
  width: 24px;
  height: 24px;
  border-radius: 999px;
  background: #fff;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  transform: translateX(0);
  transition: transform 0.18s ease;
}

.keepalive-switch input:checked + .switch-track {
  background: #5bc995;
  border-color: #48b983;
}

.keepalive-switch input:checked + .switch-track .switch-knob {
  transform: translateX(26px);
}

.switch-text {
  color: #e9f5f9;
  font-size: 13px;
}

.quick-strip {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  padding: 10px 16px 4px;
  background: var(--panel);
}

.quick-btn {
  padding: 7px 12px;
  font-size: 13px;
}

.quick-btn.danger {
  color: #c54a4a;
}

.summary {
  margin: 6px 16px 6px;
  font-size: 21px;
  font-weight: 600;
  color: #12252e;
}

.error-banner {
  margin: 0 16px 8px;
  padding: 9px 12px;
  border-radius: 12px;
  border: 1px solid #e8b7b7;
  background: #fff4f4;
  color: #b34848;
  font-size: 13px;
}

.cards-wrap {
  flex: 1;
  overflow: auto;
  padding: 8px 16px 16px;
}

.cards-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.cards-wrap::-webkit-scrollbar {
  width: 10px;
}

.cards-wrap::-webkit-scrollbar-thumb {
  background: #cad6dc;
  border-radius: 8px;
}

.profile-card {
  position: relative;
  border: 1px solid #c7d8d2;
  border-radius: 16px;
  background: var(--card-bg);
  box-shadow: var(--shadow);
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 14px 16px;
  cursor: pointer;
  animation: card-enter 0.36s ease both;
  transition:
    border-color 0.18s ease,
    transform 0.18s ease,
    box-shadow 0.18s ease;
  will-change: transform, box-shadow;
}

.sortable-item {
  width: 100%;
}

.sortable-item.sorting-item-dragging {
  position: relative;
  z-index: 12;
}

.profile-card:hover {
  border-color: #7fbea7;
  transform: translateY(-1px);
}

.profile-card.selected {
  border-width: 2px;
  border-color: var(--accent);
}

.profile-card.active {
  box-shadow:
    0 0 0 1px rgba(76, 176, 135, 0.22),
    var(--shadow);
}

.card-left-icon {
  color: #b2c1c8;
  font-size: 16px;
  letter-spacing: -2px;
}

.drag-handle {
  border: none;
  background: transparent;
  padding: 2px 3px;
  border-radius: 8px;
  cursor: grab;
  user-select: none;
  touch-action: none;
  transition: background 0.16s ease, color 0.16s ease;
}

.drag-handle:hover {
  background: rgba(91, 121, 133, 0.1);
  color: #6f8894;
}

.drag-handle:active {
  cursor: grabbing;
}

.drag-handle:focus-visible {
  outline: 2px solid #9ecab8;
  outline-offset: 1px;
}

.profile-card.dragging-source {
  opacity: 0.98;
  border-color: #8bcfb0;
  box-shadow:
    0 20px 40px rgba(43, 91, 74, 0.2),
    0 10px 20px rgba(24, 49, 62, 0.14);
  cursor: grabbing;
}

.card-main {
  flex: 1;
  min-width: 0;
}

.workspace-title {
  display: flex;
  align-items: center;
  gap: 10px;
  min-width: 0;
}

.profile-no {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 30px;
  min-width: 42px;
  padding: 0 12px;
  border-radius: 999px;
  border: 1px solid #a7d5bf;
  background: rgba(76, 176, 135, 0.14);
  color: #1e6d4d;
  font-size: 16px;
  font-weight: 700;
  line-height: 1;
  flex: 0 0 auto;
}

.workspace-name {
  color: #0c1f27;
  font-size: 31px;
  font-weight: 700;
  line-height: 1.05;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  min-width: 0;
  flex: 1 1 auto;
}

.email-line {
  margin-top: 2px;
  color: #2f5060;
  font-size: 22px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.quota-line {
  margin-top: 8px;
  display: flex;
  flex-wrap: wrap;
  gap: 8px 10px;
}

.quota-pill {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 4px 10px;
  border-radius: 999px;
  border: 1px solid #d5e1e6;
  background: rgba(255, 255, 255, 0.62);
  font-size: 14px;
}

.quota-pill strong {
  color: #2f4f5b;
  font-weight: 600;
}

.quota-pill b {
  color: #10232b;
  font-weight: 700;
}

.quota-pill small {
  color: #5c7480;
  font-size: 12px;
}

.meta-line {
  margin-top: 7px;
  color: var(--muted);
  font-size: 12px;
}

.card-side {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 8px;
}

.status-row {
  display: flex;
  align-items: center;
  gap: 8px;
}

.status-pill {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 30px;
  padding: 6px 12px;
  border-radius: 12px;
  border: 1px solid #ced8df;
  background: #fff;
  font-size: 13px;
  white-space: nowrap;
}

.status-pill.good {
  color: #137352;
  border-color: #9dd0bb;
  background: #ebf8f1;
}

.status-pill.warn {
  color: #8b5a14;
  border-color: #dfc18d;
  background: #fff8e8;
}

.status-pill.bad {
  color: #be4848;
  border-color: #e3b5b5;
  background: #fff2f2;
}

.mini-icon {
  width: 30px;
  height: 30px;
  font-size: 16px;
  line-height: 1;
}

.action-rail {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  max-width: 0;
  opacity: 0;
  overflow: hidden;
  transform: translateX(8px);
  transition:
    max-width 0.28s ease,
    opacity 0.2s ease,
    transform 0.28s ease;
}

.profile-card:hover .action-rail,
.profile-card.selected .action-rail,
.profile-card:focus-within .action-rail {
  max-width: 360px;
  opacity: 1;
  transform: translateX(0);
}

.card-action {
  padding: 7px 12px;
  font-size: 14px;
  white-space: nowrap;
}

.card-action.primary {
  background: #e9f6ef;
  border-color: #aedcc5;
  color: #236d4d;
}

.card-action.danger {
  color: var(--warn);
}

.empty {
  border: 1px dashed #b8c8d1;
  border-radius: 12px;
  padding: 36px;
  text-align: center;
  color: #4f6875;
  background: #f6fafc;
}

.status-bar {
  border-top: 1px solid #d7e0e5;
  background: #f5f8fa;
  color: #2b4551;
  padding: 9px 14px;
  font-size: 13px;
}

@keyframes card-enter {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (max-width: 1080px) {
  .top-bar {
    height: auto;
    min-height: 68px;
    padding-top: 8px;
    padding-bottom: 8px;
    flex-direction: column;
    align-items: stretch;
    gap: 8px;
  }

  .top-left,
  .top-right {
    justify-content: space-between;
  }

  .summary {
    font-size: 16px;
  }

  .profile-no {
    height: 26px;
    min-width: 38px;
    padding: 0 10px;
    font-size: 14px;
  }

  .workspace-name {
    font-size: 26px;
  }

  .profile-card {
    flex-direction: column;
    align-items: stretch;
  }

  .card-side {
    align-items: flex-start;
  }

  .action-rail {
    max-width: 360px;
    opacity: 1;
    transform: none;
  }
}

